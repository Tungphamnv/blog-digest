name: Blog Digest

on:
  schedule:
    # Chạy 1 lần/ngày lúc 23:00 UTC = 6:00 sáng giờ Việt Nam (hôm sau).
    # 1 bản tin gộp/ngày đọc tập trung hơn 12 lần ping lẻ tẻ.
    # Lưu ý: cron của GitHub Actions có thể trễ vài phút, đặc biệt giờ cao điểm.
    - cron: "0 23 * * *"
  # Cho phép bấm chạy tay từ tab Actions để test
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Test only: do not mark Gmail read or update state.json"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

# Cần quyền ghi để commit state.json ngược vào repo
permissions:
  contents: write

# Không cho 2 lần chạy chồng lên nhau (tránh ghi đè state)
concurrency:
  group: blog-digest
  cancel-in-progress: false

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Cài đặt thư viện
        run: pip install -r requirements.txt

      - name: Chạy tóm tắt
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          # Tùy chọn — chỉ cần nếu muốn đọc newsletter từ Gmail
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        run: python summarize.py

      - name: Commit state + archive (nếu có thay đổi)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add state.json
          if [ -d archive ]; then
            git add archive
          fi
          if git diff --staged --quiet; then
            echo "Không có gì thay đổi, bỏ qua commit."
            exit 0
          fi
          git commit -m "Cập nhật state + archive [skip ci]"
          # Nếu remote đã có commit mới (agent khác vừa push), push sẽ bị từ chối
          # "fetch first". Khi đó kéo commit mới về + rebase rồi thử lại, tối đa 5 lần.
          for i in 1 2 3 4 5; do
            if git push; then
              echo "Push thành công."
              exit 0
            fi
            echo "Push bị từ chối, kéo commit mới rồi thử lại (lần $i)..."
            git pull --rebase origin "${GITHUB_REF_NAME}"
          done
          echo "Push thất bại sau 5 lần thử." >&2
          exit 1
